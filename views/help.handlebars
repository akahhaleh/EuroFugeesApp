<!doctype html><html><head>{{> globalHead}}</head>
<body>
  {{> globalNavigation}}
  <div class="container">
    <h2 class="text-center">Help</h2>
    {{#each questions}}
    <div class="help-topic" id="{{id}}">
      <a href="#">
        <h3>{{topic}}</h3>
      </a>
      <p>{{answer}}</p>
    </div>
    {{/each}}
  </div>
  {{> globalBottomScripts}}
</body></html>